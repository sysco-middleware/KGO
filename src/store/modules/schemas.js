const state = {
  schemas: {}
}

const getters = {
}

const actions = {
  async fetch ({commit}, id) {
  }
}

const mutations = {
  setSchema () {

  }
}

export default {
  namespaced: true,
  mutations,
  getters,
  actions,
  state
}
